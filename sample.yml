---
  - hosts: all
    become_user: ansible
    become_method: sudo
    gather_facts: true
    connection: ssh
    tasks:
      - name: create new directory
        file: path=/opt/chaithu state=directory
